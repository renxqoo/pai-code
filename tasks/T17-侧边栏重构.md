# 侧边栏重构（分组/项目双视图 + 置顶） 方案

> 状态：已实施（对抗审查 P1×2/P2×3/P3×5 处置完毕，真机交互验证 + 视觉走查通过）
> 级别：中
> 参考稿：`sess_2c76fa3f…/image-6182…png`（分组视图）、`image-cb8e…png`（项目视图）——UI 按 1:1 还原

## 契约

- **零 hub 协议变更**：纯渲染层重构（数据全部来自既有 `SessionView` / `preferences`）
- 置顶复用 `preferences.pinnedSessions`（key = `sessionPath`，与设置页已保存列表同一真相；`null` sessionPath 的会话〔首条消息前〕不提供置顶入口）
- `SessionCardModel` 变更（渲染层内部模型，非协议）：
  - 删 `conversationId`（全仓零读取，只写不读的冗余字段）
  - 增 `sessionPath: string | null`（置顶键）
  - 增 `streaming: boolean`（行内活动指示数据源）
- strings 新 key（中英同步）：快捷区（新建任务/搜索/自动化/插件市场 + ⌘N/⌘K 徽标，修饰键符号按平台注入）、视图（分组/项目）、已置顶、置顶/取消置顶、显示更多、进行中、收起侧栏、相对时间标签（刚刚/N分钟/N小时/N天）；侧栏不再使用的旧 key 同步删除（allProjects/newProject/newThread）

## 问题域

- 处理：
  - 双视图数据编排：**分组视图**（置顶区 + 全量会话按 `lastActivityAt` 倒序平铺，每项相对时间标签）；**项目视图**（置顶区 + 按 `projectName` 分组，文件夹行可折叠、组内倒序、超限折叠 + 显示更多）
  - 置顶/取消置顶（hover 钉子，读写 `pinnedSessions`）
  - ⌘N 新建任务（NewThreadModal）、⌘K 搜索聚焦（内联搜索框展开 + 聚焦；已展开态再次触发重新聚焦）
  - 项目组折叠面：文件夹行单组折叠（折叠联动重置该组「显示更多」展开态）与「显示更多」展开
- 不处理（挂账）：
  - **底部功能按钮区保持现状不动**（用户裁决：settings/workflows/usage + refresh 不改；设计稿底部用户区〔头像/电话/齿轮〕不还原）
  - 工具栏 `#`（标签筛选）与垃圾桶（清理）图标——无标签/清理数据面，按仓库既有占位模式 noop，接线点保持稳定；本任务不做标签系统
  - 已保存未打开会话在侧栏展示（含置顶区）——置顶区只显示 live 会话；已关闭会话仍在设置页已保存分区（行为不变）

## 并发/一致性预算

- 相对时间标签沿用分钟级独立 tick（30s），不随流式 tick 重渲侧栏
- 折叠集合（项目组折叠、显示更多展开）为纯 UI 态，语言切换根级重挂载经 `uiState` 持久；视图切换（分组/项目）同 `uiState` 持久
- 置顶写路径复用 `updatePreferences` 串行链（既有），无新增并发面

## 拆分

逻辑层（主 agent 实现）：

| 文件 | 职责 |
| --- | --- |
| `sidebar/session-card-model.ts` | 模型变更（±字段如上） |
| `sidebar/build-pinned-list.ts` | 置顶交集（sessionPath ∈ pinned）+ 倒序 |
| `sidebar/build-time-list.ts` | 分组视图平铺（排除置顶，lastActivityAt 倒序） |
| `sidebar/build-project-groups.ts` | 项目分组（组内倒序；`SHOW_MORE_LIMIT` 折叠 + expanded 集合展开） |
| `sidebar/format-sidebar-age.ts` | 相对时间标签（刚刚/N分钟/N小时/N天，文案注入） |
| `sidebar/group-collapse.ts` | 折叠面代数（GroupFold 双集合：文件夹折叠 toggle 联动清「显示更多」、expandGroup 幂等展开） |
| `sidebar/rename-commit.ts` | 行内重命名提交值裁决（旧卡片语义纯函数化钉回归） |
| `hooks/cmd-hotkeys.ts` | ⌘N/⌘K 窗口级监听（输入框内安全：带修饰键组合） |
| `live/use-live-workspace.ts` | `toCards` 补 sessionPath/streaming、删 conversationId |
| `screens/workspace-main.tsx` | 装配双视图 + 置顶 + 快捷键 + 底部区动作迁移 |
| `strings/{zh,en}.ts` | 新 key |

UI 层（ui-coding 子 agent，按参考稿 1:1）：

| 文件 | 职责 |
| --- | --- |
| `sidebar/sidebar.tsx` | 重写：快捷区 → 视图切换行 → 列表（置顶 + 双视图）→ 底部用户区 |
| `sidebar/quick-actions-row.tsx` | 新建任务(⌘N)/搜索(⌘K)/自动化/插件市场 四行入口 |
| `sidebar/sidebar-search-input.tsx` | 内联搜索框（展开态，Esc 收起清空） |
| `sidebar/view-switch-tabs.tsx` | 分组｜项目 胶囊分段控件 + 右侧三小图标（收起侧栏/# 占位/垃圾桶占位） |
| `sidebar/session-row.tsx` | 单行会话（标题 + 相对时间 + hover 动作：钉/重命名/关闭 + streaming 指示） |
| `sidebar/pinned-section.tsx` | 已置顶分组（斜钉图标行） |
| `sidebar/project-section.tsx` | 项目视图分组树（文件夹行 + 缩进行 + 显示更多） |

（组件清单允许子 agent 按「一组件一文件」微调拆分，功能面不得增减；底部 `sidebar-footer.tsx` 保持现状。）

## 实施顺序

1. 方案定稿（本文档）
2. 逻辑层纯函数 + 模型变更 + strings + 单测（主 agent）
3. UI 组件按稿还原 + `renderToStaticMarkup` 冒烟（子 agent，传参考稿路径）
4. 装配接线 + 快捷键（主 agent）
5. 四门（lint 0-0 / typecheck / build / test）+ dev 运行截图对照设计稿
6. 对抗审查（独立会话审 diff）+ 偏差处置
7. 提交（本任务文件全量点名）

## 裁决

- 默认视图 = 分组；两视图共用置顶区（置顶项不出现在下方列表）
- 显示更多：每组默认 5 条，展开后整组全量；折叠文件夹行联动清该组展开态（再展开回到 5 条截断）；不提供行内「收起」按钮（照稿单向）
- 底部功能按钮区不动（用户裁决）；工具栏三小图标：收起 = 折叠侧栏（两视图皆有效，与标题栏开合同一状态位，收起时联动收起搜索框），`#`/垃圾桶 = 纯视觉占位（aria-hidden 装饰钮，无数据面故不做可聚焦 noop 钮；接线时再替换为真控件）
- 搜索为行入口展开式（非常驻输入框）：点击「搜索」行或 ⌘K 展开内联输入框并聚焦（已展开时重新聚焦）；Esc 收起并清空——Esc 分发链次序 = 对话框（模态层最优先）→ 侧栏搜索 → Usage → 新会话弹窗 → 设置 → 面板 → bash/停止
- 项目分组排序：组内与组间均按最近活跃倒序（输入顺序不构成前提）；显示更多 limit 钳制非负
- 侧栏默认宽度 264（可拖 208~400）；时间标签粒度：刚刚(<60s) / N分钟(<60m) / N小时(<24h) / N天(≥24h)，未来时间按刚刚

## 测试口径

- `build-pinned-list`：交集/倒序/空 pinned/过滤查询联动/无 sessionPath 不入
- `build-time-list`：排除置顶、倒序、空输入空数组
- `build-project-groups`：分组键、组内倒序、组间按各组最近活跃倒序（输入乱序不构成前提）、limit 截断与 0/负数边界、expanded 全量、空组不出现、过滤联动
- `format-sidebar-age`：边界表驱动（0s/59s/60s/3599s/3600s/86399s/86400s/多天/未来）
- `group-collapse`：折叠联动清展开态（再展开回截断）、切回展开不动显示更多、expandGroup 幂等、输入集合不动
- `rename-commit`：trim 提交/空=取消/同名=取消（trim 后比较）/空原标题可命名
- `resolveCmdHotkey`：⌘/Ctrl 命中、无修饰与 Alt 不劫持、大小写形态
- `esc-action`：侧栏搜索收起优先级（对话框之后、面板/停止之前）
- 组件冒烟：Sidebar 双视图 `renderToStaticMarkup` 不抛错、关键节点在位（快捷区/徽标/Tab/置顶区/显示更多/空态/footer/流式指示）
- 既有 `filter-sessions` 用例保留不动

## 对抗审查处置（已实施轮）

独立会话审 diff + 规格 + 旧实现，P1×2 / P2×3 / P3×5，全部处置：

- **P1 文档残留（折叠全部/展开全部）**：裁决演化为「收起 = 折叠侧栏」后四处文档提及未同步——已删净（方案与代码同变）。
- **P1 折叠不重置显示更多**：裁决「再折叠文件夹行重置回 5 条」未实现——重构为 `GroupFold` 双集合（`toggleGroupFold` 折叠联动清展开态、`expandGroup` 幂等），带症状回归用例。
- **P2 ⌘K 已展开态不重聚焦**：`searchFocusToken` 递增信号 + `SidebarSearchInput` effect 重聚焦。
- **P2 迁移语义无回归钉**：`rename-commit.ts` 纯函数化（trim/空/同名取消语义）+ 用例；新增 hotkey/esc-action/组排序用例。
- **P2 收起侧栏后搜索残留吞 Esc 一拍**：`collapseSidebar` 联动 `closeSidebarSearch`。
- **P3 快捷键徽标硬编码 ⌘**：`lib/platform` 出 `MODIFIER_KEY_LABEL`（⌘/Ctrl），文案 key 函数化注入。
- **P3 组间顺序依赖输入**：组间改按各组最近活跃倒序（自足），注释与用例同步。
- **P3 limit 负数静默去尾**：入口钳制 `Math.max(0, limit)` + 负数边界用例。
- **P3 占位钮形态**：`#`/垃圾桶为 aria-hidden 纯视觉占位（裁决措辞已同步：无数据面不做可聚焦 noop 钮）。
- **P3 Esc 链措辞**：文档「最前」改为「对话框（模态层）之后、其余面板之前」。
- 实施期自查另修真机交互缺陷：**项目视图行漏传 `onTogglePin`**（ProjectSection 未声明/未透传，真机聚焦行复验发现）——已补三处钉子渲染条件一致。
- 审查确认过（无动作）：旧卡片重命名/关闭/键盘可达语义逐条等价迁移、置顶三处渲染条件一致、pinnedSessions 与设置页同源同键、memo 链与 uiState 快照依赖全量、Esc stopPropagation 机制与旧搜索行同假设。

## 验收清单

- [x] 分组视图：快捷区(⌘N/⌘K 标注) + Tab(分组选中) + 已置顶 + 时间倒序平铺 + 相对时间标签
- [x] 项目视图：Tab(项目选中) + 已置顶 + 项目分组树(缩进/折叠联动/显示更多/组间按活跃排序)
- [x] 置顶：hover 钉子切换（两视图行内入口一致），设置页已保存列表同步同一真相
- [x] ⌘N 开新任务弹窗、⌘K 展开/重聚焦搜索、Esc 先收搜索（对话框之后最优先）
- [x] 底部功能按钮区保持现状（settings/workflows/usage + refresh 均可用）
- [x] lint 0-0 / typecheck / build / test 全绿（602 用例，基线 555 → +47），覆盖率不降
- [x] 对抗审查偏差清单清零（P1×2 / P2×3 / P3×5 全处置，见上节）
- [x] 真机交互验证：视图切换/⌘K 搜索过滤/Esc/显示更多/置顶-取消置顶全链路（dev 实例实测）
- [x] 视觉走查：双视图静态渲染对照设计稿逐项符合（快捷区/Tab 胶囊/斜钉/缩进/显示更多/footer）
