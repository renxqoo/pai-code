# T41：渲染层门面收敛——「点击发送只调一个函数」（讨论稿，未定稿）

> 起因（2026-09-21 用户讨论）：「比如我点击发送按钮，我只要调用封装好的发送按钮就行了，
> 之前封装的代码有问题，可以直接重构。」本稿只记现状解剖、目标形态与开放问题；
> 不写代码。建议时序：T40 W2（ApiError kind 文案表落地）之后启动——文案层先统一，
> 表面合并才不返工。

## 0. 现状解剖：一次「发送」穿过的模块（7 跳）

```
UI（ComposerRegion 发送按钮/回车）
 → composer/submit-composer-draft.ts   草稿清理编排
 → composer/submit-draft.ts            `! ` bash 前缀检测 + 空校验（deps.actions 反向注入——层次倒挂）
 → live/workspace-actions.ts           notifySubmitFailure 文案映射（面 65 个成员）
 → live/live-controller.ts             守卫/懒恢复/自愈/prompt 投递
 → live/client-invoke.ts               IPC
 → main api-routes 'session/prompt'    compact 词法拦截 / streaming_window 降级重试
 → hub
```

四个病灶：
1. **一个动词穿 5 个渲染层模块**；`! ` 直执行检测在 composer 层（应是发送服务内部路由）。
2. **层次倒挂**：submit-draft.ts 接收 `deps.actions` 回调上抛——底层模块调用上层动作。
3. **双表面动词二写**：workspace-actions 与 live-controller 每个动词各写一遍
   （submitDraft ×2、selectThinking ×2、stop ×2……），65 成员大杂烩面。
4. **文案映射散三处**：notifySubmitFailure（workspace-actions）、controller 内 pushNotice、
   strings 内 createFailed 分派——T40 W2 的 kind 查表会先统一这一层。

## 1. 目标形态

```
UI：onClick={() => void pai.send(draft)}        // 一行，唯一调用

pai.send（一个模块拥有整个动词的管线）：
  空校验 → `! ` 路由（→ pai.bash）→ 空舞台守卫 → 懒恢复 → IPC
  → unknown_thread 自愈 → kind→文案 notice → 草稿清理约定
  返回 SendOutcome = { ok: true } | { ok: false; error: ApiError }（调用方按需附加 UI 反馈）
```

- **读写分离**：UI 读状态走 store 订阅（现状保持），写动作只走 pai 动词。
- **两层合一处一动词**：UI 门面层（pai：动词 + 文案呈现）+ 会话管线层（守卫/恢复/自愈/
  排队的内部实现）。workspace-actions 与 live-controller 的二写收敛；composer/submit-* 与
  queued-* 并入发送管线（流式排队是 send 的一部分，不是独立模块）。
- pai 面按域组织：发送域（send/bash/stop/steer）、会话管理域（createTask/openSaved/close/
  rename/fork/delete）、选择域（selectModel/selectEffort/setPermissionMode）、本地能力域透传。

## 2. 开放问题（待讨论定稿）

1. **notice 归属**：facade 内推（现状行为，UI 零样板）vs 返回由 UI 呈现（更纯但每个调用点
   样板）——倾向「facade 推 + outcome 返回」并存。
2. **pai 形态**：单例对象（workspace-runtime 先例）vs hook（usePai()）——倾向单例对象，
   测试注入与现状一致。
3. **65 成员面的拆域清单**与 UI 调用点清扫顺序（R3 波）。
4. **与 T40 的界面**：T41 只动渲染层与 workspace/actions 合并，不碰协议与主进程；
   依赖 T40 W2 的 ApiError/kind 文案表。

## 3. 波次草案（讨论用，未定稿）

- R1 发送管线合并：submit-*、queued-* 并入会话管线；`! ` 路由内置；层次倒挂消除。
- R2 双表面合并：workspace-actions 并入门面层（文案统一为 kind 查表）；动词二写消除。
- R3 UI 调用点清扫：全 UI 只 import pai（grep 收口：UI 层不再出现 controller/actions 引用）。
